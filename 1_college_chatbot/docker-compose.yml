version: "3.9"

services:
  college-chatbot:
    build: .
    container_name: college-chatbot
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./database:/app/database
      - ./logs:/app/logs
    restart: unless-stopped
